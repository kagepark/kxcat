####################################
# License : GPL
####################################

cp() {
    (( $# < 3 )) && error_exit "${FUNCNAME} <group name|hostname> <src file> <dest dir>"
    xdcp $@
}
